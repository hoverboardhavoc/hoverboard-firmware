<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Android 12+ BLE permissions. neverForLocation means no location grant is needed for SCAN on
         12+ (specs/ble.md, "Drive mode"). -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Pre-Android-12 (e.g. the ASUS ROG on Android 8): the legacy BT perms + location for BLE scan.
         maxSdkVersion 30 so they do not apply on 12+ (which uses the new runtime perms above). -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <application
        android:allowBackup="false"
        android:label="BLE Throughput Test">
        <activity
            android:name=".RunnerActivity"
            android:exported="true" />
    </application>
</manifest>
